# DESIGN.md — 学生管理系统


---

## 1. 我怎么理解这门生意

**这不是一个学生 CRUD 应用，而是一套预付费履约系统。**

一个课时同时是三样东西：**家长已经付掉的钱**（机构背着的一笔负债）、**机构可以卖的库存**、以及**对老师作出的排课承诺**。下面所有内容都从这一句推出来。把它建模成 CRUD，恰好丢掉了唯一重要的东西——*这些课时从哪来、到哪去*。

所以重心是**课时账本（credit ledger）**，而不是学生档案。

### 谁在用，以及每个人真正怕什么

| 角色 | 规模 | 他真正怕的 | 这对界面意味着什么 |
|---|---|---|---|
| **admin**（销售顾问 + 排课，合并为一个角色） | 10 人，每人约 100 名学生 | 不是“我找不到数据”，而是**“我忘了行动”**。试听完那通没打的跟进电话；没人提、直到课时归零才被想起的续费。 | 首页必须是**两条待办队列**，不是学生列表。列表用于你已经知道要找谁；队列用于你不知道今天该干什么。 |
| **老师** | 20 人，每周约 60 节课 | 在两节课之间用它约 30 秒。怕的是不知道今天这个班里**谁是第一次来**、**谁已请准假**。也怕因为课时对不上而被追责。 | 请假状态从系统预填；老师只处理**例外**，每一次覆盖都带着操作人写进账本。 |
| **学生 / 家长**（共用家庭账号） | 约 1000 名学生 | **“我的课时不见了，没人说得清为什么。”** | 不要显示 `balance = 12`，要显示 `balance = 12，因为这 8 条账本记录`。 |

### 生命周期

```
询盘 → 试听 → [AI 转化卡] → 跟进（48h SLA）→ 付费报名（钱进）
     → 编入周课 → 出勤 → 出勤结算 → 扣课时（钱出）
     → 课时不足预警 → [AI 续费卡] → 续费（钱再进）或流失
```

两个 AI 触点恰好落在钱**进**来的两个时刻上。这是刻意的。

---

## 2. 我要解决什么，以及我刻意不解决什么

### 范围内

**P0** — 登录 + 三级权限 · admin 待办队列 · 购买与账本开账 · 三重校验的报名 · 出勤结算与扣课时 · 课时账本 · 试听→跟进闭环 · AI 续费卡。

**P1** — AI 转化卡 · 家庭视图（课时、课表、反馈、请假申请）· 班级维护。

### 明确不做

| 不做 | 原因 | 何时重新考虑 |
|---|---|---|
| 支付网关 | 演示陷阱：要引入服务商、webhook、对账，而评分价值为零 | 这个作业永远不做 |
| 管理看板 / 图表 | 题目已明确批评“一张表一个 CRUD 页”。图表是给老板看的，不是给上述三个角色 | 等有了“老板”这个角色 |
| 邮件 / 短信 | 需要服务商 + 模板 + 送达追踪 | v2 |
| 补课、调课 | 复杂度高、MVP 价值低——**但模型仍必须回答“老师请了一周假”**（见 R10） | v2 |
| 拖拽排课 | 难的是*校验*，不是拖拽 | v2 |
| 家长小程序 / 多校区 / 1 对 1 定价 | 范围 | v2 |
| 课时包过期强制 | 字段存在（`expires_at`），但刻意不做校验——一个不校验的字段是诚实的，一条错误强制的规则是客服工单 | v2 |

---

## 3. 数据模型

16 张表。完整 DDL 在 `backend/migrations/`。

| 表 | 用途 | 关键字段 |
|---|---|---|
| `users` | 登录主体，`role ∈ admin/teacher/student` | `username` UK、`password_hash` |
| `students` | 生命周期主体 | `owner_admin_id`、`user_id`（家庭登录）、`status ∈ lead/trial/active/churned` |
| `guardians` | 家长联系方式，**不带凭据** | `student_id`、`relationship`、`is_primary` |
| `subjects` | 科目 | `name` UK |
| `teacher_profiles` | 老师属性 | `user_id`、`active` |
| `classes` | 每周重复的班 | `subject_id`、`teacher_id`、`weekday`、`start_min`、`end_min`、`capacity` |
| `class_enrollments` | 学生 ↔ 班 | `weekday`、`start_min` 冗余存储，用于唯一性守护 |
| `lessons` | 班的一次带日期实例 | `lesson_date`、`status ∈ scheduled/cancelled/completed` |
| `attendances` | 每个学生每节课 | `status`、`source ∈ prefilled/teacher_override/system`、`recorded_by_user_id` |
| `leave_requests` | 家庭提交的请假 | `resolution ∈ approved_ge_24h / late_lt_24h` |
| `credit_packages` | 一次购买 | `total_credits`、`price_cents` |
| **`credit_ledger`** | **只增不改的课时流水** | `delta`、`reason`、`lesson_id`、`actor_user_id`、`created_at` |
| `trials` | 试听 | `UNIQUE(student_id, subject_id)` |
| `follow_ups` | 跟进任务 | `due_at`、`status` |
| `ai_decisions` | AI 卡的审计留痕 | `kind`、`output_json`、`ai_status` |
| `audit_events` | 归属转移等 | `actor_user_id`、`payload_json` |

### 题目特别问到的三组关系

**学生 ↔ admin。** `students.owner_admin_id` 是一条归属边，不是一个权限缓存。admin 可以**读所有人**（他们需要这样，才能发现重复询盘），但**只能写自己的**。R7 在每次请求上重新读 `owner_admin_id`，从不缓存——所以转移一个学生立即生效，不存在残留权限。转移本身只是一条改 `owner_admin_id` 的 `UPDATE`；报名、账本、出勤都不动，历史因此保持完整。

**班 ↔ 课。** `class` 是*每周重复的时间槽*（`weekday` + `start_min`/`end_min`）。`lesson` 是它的*带日期实例*。这一点重要，是因为它让“老师请一周假”变得可解：把那一周的 `lesson` 行取消，就永远不会产生出勤记录、也不会产生任何课时变动。按学期固定课表没有建模——按日期实例化的结构以后能吸收它，但 MVP 不需要。

**课时怎么记、怎么扣。** **没有 `students.credit_balance` 这一列**。余额 = `SELECT COALESCE(SUM(delta),0) FROM credit_ledger WHERE student_id = ?`。冗余的余额列必然会和账本漂移，而在这里漂移就是一次账单纠纷。

- 钱进：购买追加 `reason='purchase'`、`delta = +N`
- 钱出：出勤结算追加 `reason='consume'`、`delta = -1`
- 退款 / 补偿 / 机构停课：追加一条**反向记录**（`refund`、`manual_adjust`、`leave_adjust`）——绝不改、绝不删已有行

`credit_ledger` 的“只增不改”是在**三层**上构造出来的：

| 层 | 手段 | 它挡住了什么 |
|---|---|---|
| 数据库 | `CHECK(delta <> 0)` · 没有 `updated_at`、没有 `deleted_at` · `UNIQUE(student_id, lesson_id, reason)` · 应用账号只授 `SELECT, INSERT` | 哪怕直连数据库也改不了历史；结算按钮连点两次也不会重复扣费 |
| 仓库层 | `CreditRepository` **只**暴露 `Insert` 和 `SumBalance`——没有 Update 或 Delete 方法可调 | 任何开发者，而且在编译期就被挡住 |
| 服务层 | 唯一入口 `CreditService.Apply`，强制要求 `actor_user_id` 和白名单内的 `reason`；每个写课时的事务都以 `SELECT id FROM students WHERE id=? FOR UPDATE` 开头 | 无审计的写入；并发重复扣减 |

**为什么“只增不改”不是过度设计：** 预付费消费场景下，经营者持有交易记录而拒不提供的，采信消费者一方的说法。所以只增账本不是技术偏好——它是机构**在举证上的位置**，而家庭侧看到的账本视图就是这个位置对用户可见的形态。

---

## 4. 系统必须强制的规则

十一条。每一条都在**服务端**强制；前端只反映状态。

| # | 规则 | 数据库层 | 服务层 | 错误码 |
|---|---|---|---|---|
| R1 | 每个学生每个科目只能试听一次 | `UNIQUE(student_id, subject_id)` | 预检查 | `40905` |
| R2 | 试听必须在 48h 内跟进 | — | 写结果时在同一事务里建 `follow_ups.due_at = now + 48h`；查询端推导逾期 | — |
| R3 | 报名校验三件事：周时间槽重叠、班级容量、余额 > 0 | `UNIQUE(student_id, weekday, start_min)` | 事务：锁父行 → 区间相交 → 容量 → `SUM(delta) > 0` | `40901/40902/40903/40904` |
| R4 | 出勤结算课时：present/late/absent/开课前 24h 内的请假各扣 1；开课前 ≥24h 的请假扣 0 | `UNIQUE(student_id, lesson_id, reason)` 幂等 | 状态机 + 24h 阈值 | `42201` |
| R5 | 账本只增不改；余额 = `SUM(delta)` | 见上面三层 | `Apply()` 是唯一写入者 | — |
| R6 | 余额 ≤ 4 生成续费任务；余额 = 0 阻止报名 | — | 报名事务第 (d) 步 | `40904` |
| R7 | admin 可读全部学生，只可写 `owner_admin_id = self` 的 | — | 中间件每次请求重读归属 | `40301` |
| R8 | AI 输出必须过枚举白名单；失败降级，绝不打挂 | — | 校验 → `ai_status = unavailable` | `42202` |
| R9 | 课时包过期字段存在，不做强制 | `expires_at` | 不校验 | — |
| R10 | 老师请假：取消那一周的 `lesson` 行；**无出勤、无课时变动** | — | 批量取消；已完成的课不可取消 | `409` |
| R11 | 归属转移可审计 | — | `audit_events` 记录 from/to/actor | — |

两条关于强制边界的诚实说明：

- **起始时间不同的重叠时间槽**无法用 MySQL 约束挡住。Postgres 的 `EXCLUDE USING gist` 可以；MySQL 没有等价物。它靠一条事务内查询兜住——所以每次报名都跑在同一个带行锁的事务里，而不是指望间隙锁（MySQL 8 的 `READ COMMITTED` 范围锁不阻止插入）。
- **R4 的“谁来批准请假”**：没有人工审批。提交的瞬间由服务端按 24h 阈值判定。加一个人工审批步骤会引入一个定义不清的“待审批”状态，更糟的是，它挡不住有人绕过界面前来上课。

---

## 5. 我的假设，以及我会问什么

### 题目留白处我的假设

1. “10 个 admin（课程顾问 / 教务）”是**一个合并角色**，不是两个。所以权限不对称是*全读 / 只写自己*，而不是基于角色。
2. 家长和学生**共用一份家庭凭据**，挂在 `students` 上。K-12 场景里家长才是真实用户；一份凭据胜过多一份凭据再加一张授权图。
3. 每周 60 节课意味着**把周重复班展开成带日期的实例**，而不是一张学期固定课表。
4. 迟到仍然扣课时（座位和老师的时间都已经占掉了）。
5. 允许负余额（已排的课仍然要上），但会触发透支预警；被挡住的是余额为零时**报名**（R6）。
6. 所有时间都是 `Australia/Melbourne` 墙上时间。任何地方都不做转换。

### 我会问业务的五个问题

1. “一次试听”是按学生×科目，还是按学生×老师？（换一个老师再试听是说得通的。）
2. 48h 跟进 SLA 含周末吗？澳洲的辅导中心周六上课，所以自然小时和营业时间差别很大。
3. 老师请假一周时：退课时、课时顺延，还是照扣？
4. 这 10 个 admin 是在抢同一批学生，还是在协作？这决定“全读”到底是对的，还是太松。
5. 课时包会过期吗？赠送的课时在退款时怎么计价？

---

## 6. 页面与信息结构

**六个页面。** 我刻意*没有*做登录页之外的出勤页、课时页、请假页或角色页——每张表不配拥有自己的一块屏幕。

| 角色 | 落地页 | 首屏 | 到高频动作要几步 |
|---|---|---|---|
| admin | `/today` | 逾期跟进 · 今明两天的试听 · 课时不足预警 | 催续费：**1** 步（行内按钮）。记跟进：**1** 步（抽屉） |
| 老师 | `/teach/today` | 下一节课、还有多久 · **几个新面孔、几个请假** · 昨天点名有没有漏 | 点名：**2** 步。改一个状态：**1** 步（行内） |
| 家庭 | `/me` | 剩余课时 + 消耗速度 + 预计用完日期 · 最近 4 条流水 · 请假申请 | 提交请假：**2** 步 |

状态用**三条冗余通道**编码——颜色、形状、文字。验收标准：把截图转成灰度后仍然可读。只有一个彩色圆点，评审不通过。出勤有**六**个状态，且 `unrecorded` 是显式的第六态而不是空格子，因为空格子既点不了、也统计不了。

> 下面三张草图是**真实界面的英文示意**：产品 UI 本身就是英文的，草图与实现保持一致。草图不计入 3 页。

### admin 工作台 `/today`

```
+------------------------------------------------------------------------------+
| Austin Education   [ search students / classes / leads            ]  Mei Lin |
+-------------+----------------------------------------------------------------+
| Today     * |  Tue 19 Aug - Melbourne     [Today v] [Mine: 100]   density    |
| Leads     9 |                                                                |
| Students 100|  ! Follow-ups overdue  3                            All 12 >   |
| Classes  18 |  +-----+------------+--------------------+---------+---------+ |
|             |  | (o) | Chen, Eric | 14 Aug trial-lost  |2d over  | Call    | |
| Saved view  |  | (o) | Wang, Amy  | 15 Aug trial-maybe |1d over  | Message | |
|  > Low    6 |  +-----+------------+--------------------+---------+---------+ |
|  > Unpl.  2 |  o Today/tomorrow trials  4                         All 9 >    |
|             |  +-----+------------+--------------------+---------+---------+ |
|             |  | (.) | Zhao, Nina | Today 16:00 Begin  |Zhou     | Details | |
|             |  +-----+------------+--------------------+---------+---------+ |
|             |  ! Low credits  6                                   All 6 >    |
|             |  |  !  | He, Grace  | [####------]  4/40| ~2 wks   | Renew   | |
|             |  |  !  | Guo, Peter | [###  --  ] -2/40| overdrawn | Urgent  | |
+-------------+----------------------------------------------------------------+
```

区块按**紧迫度**排序，不按表排。每行都以一个具体动作收尾（*Call*、*Renew*），绝不是泛泛的 *View/Edit*。

### 老师点名

```
+------------------------------------------------------------------------------+
| <- Today    Beginner Maths A - Thu 16:00 - Week 12     [ Mark all present ]  |
+------------------------------------------------------------------------------+
|  o Sun, Leo - leave registered >=24h ahead, no credit deducted (read-only)    |
|  +--+--------------+-------+-----------------------------+--------+---------+ |
|  |N | Student      |Credits| Attendance                  |Settles | Note    | |
|  +--+--------------+-------+-----------------------------+--------+---------+ |
|  |+ | Zhu, Hannah  | 24/40 | (x) Present (c) Late (/) Absent  | -1  | New!  | |
|  |  | Sun, Leo     | 18/40 | (o) Leave - approved (locked)    | none | >=24h | |
|  |  | Ma, Chloe    |  3/40 | (0) Leave - under 24h (locked)   | -1  | ! 3   | |
|  |  | Qian, Ryan   | 31/40 | (.) Not recorded  <- click       | ?    |        |
|  +--+--------------+-------+-----------------------------+--------+---------+ |
|  Recorded 5/6 - total -4 credits        [ Save ] [ Save + feedback ]         |
+------------------------------------------------------------------------------+
```

新面孔数在列表页上就能看到——老师不该为了发现一个新生而先打开这个班。**Settles** 这一列在老师点下的那一刻就告诉他，刚才动了多少钱。

草图上那个 `[ Save + feedback ]` 没有做成第二个按钮，而是**并进了表格**：最后一列本身就是备注框，老师提交前直接写下这节课的表现，随同一次提交落库。理由是 §6 那张表的标准——点名要在两节课之间 30 秒内做完，多一个按钮就是多一次决策；而"记反馈"本来也不需要单独的时刻，它就是点名的一部分。这一列因此有三个读者：老师自己、**AI 续费卡**（见 §7）、以及后来在学生抽屉里读原话的顾问。它是老师那一屏唯一能自由输入的字段，提交后转只读——纠错走 `PATCH`，而 `PATCH` 不接受备注，所以界面不会留一个改了不生效的框。

### 家庭页 `/me`

```
+--------------------------------------------------+
| Zhao Min (parent) - Noah Zhao            Alerts 3 |
+--------------------------------------------------+
|  Remaining credits                               |
|        12                 [######------------]   |
|        / 40               used 28 - left 12      |
|   At 2/week that is about 6 weeks (2 Oct)        |
|        [ Request leave ]   [ Credit history ]    |
+--------------------------------------------------+
|  o Leave registered - no credit deducted         |
|    8/23 Sat 16:00 - Beginner Maths A             |
|    41h before start (>=24h) -> not deducted      |
+--------------------------------------------------+
|  History                                         |
|  8/19  Beginner Maths A   -1   Present           |
|  8/15  Beginner Maths A    0   Leave >=24h       |
|  8/12  AEIS Writing       -1   Leave <24h        |
|  8/10  -                 +40   Renewal - 40 pack |
+--------------------------------------------------+
```

每一行流水都说清了数字**为什么**变。这是对“我的课时不见了”的正面回答。

---

## 7. 为什么切这一刀

我选了**试听 → 购买 → 报名 → 出勤 → 结算 → 课时不足预警 → 续费**这条完整链路，而不是更窄的一刀，因为它是能证明整个设计所依赖的那一个念头——**课时就是钱**——的最短路径。

切得更窄就断了证据。只做试听，只看到钱进来，永远看不到它被消耗；只做出勤，只看得到消耗，永远看不到课时从哪来。只有完整链路能看到钱进 → 钱出 → 快用完 → 钱再进，也只有完整链路能端到端跑完这十一条规则。

它同时也给了 AI 一份真活。续费卡的输入——出勤率、近期反馈、消耗速度、距上次续费的时间——全都由这条链路产生，所以喂给模型的是系统真正拥有的数据，而不是编出来的字段。**「近期反馈」特指老师亲手写下的那些字**：`attendances.note` 这一列同时被两条系统路径写（迟请假的判定语、纠错的审计后缀），照原样喂给模型，它会拿机构自己的样板文字当证据输出 `feedback_negative`——一个背后没有观察者的风险标签。所以读之前先剥掉系统文本，剥完为空的整行丢弃；顾问侧读同一份数据、走同一套清洗，于是卡片里和学生抽屉里永远是同一个版本。又因为 48h 跟进 SLA 和课时不足预警都是**规则**、不是 AI 输出，模型不工作时系统照常运行：调用失败就置 `ai_status = 'unavailable'`，确定性的卡在同一个位置、同样的高度、同样的按钮上渲染。演示会刻意走一遍这条失败路径，而不只是顺利路径。

这一列**只有内部读者**：老师自己、AI 续费卡、顾问的学生抽屉。家长读的是**另一份数据**——顾问处理完一次问题后亲手写下的那句话（`follow_ups.parent_note`，家庭端 `/me` 的「课堂反馈」）。两者**不互相派生**，不做自动脱敏、不做自动摘要：漏一条就是机构少说了一句，而"忘了说"和"没什么可说"必须能区分开——后者就是这条记录不存在。同一个学生，内部写着 `Said he does not want to come anymore. Worth a call home before next week.`，家长读到的是 `…so I rang home the same afternoon. We are sitting down properly on Thursday…`：**问题没有被隐瞒**，隐掉的是写给同事的那半句便条，补上的是"我们已经在做什么"。seed 的 15 条课堂记录里，7 条有对外话、3 条被老师标记为「需要顾问跟进」而顾问还没处理、剩下 5 条只在内部记一下——**这个比例本身就是设计**。老师端那一列旁边只有一个勾、不是第二个输入框：老师不需要决定对家长说什么，只需要把「这件事得有人接手」推出去，服务端据此在顾问名下开一条待办（`follow_ups.source='teacher_note'`）；顾问处理完再写那句对外的话。顾问不是等到课时快用完才维护，而是每次出问题就维护、并顺手交代一句；续费于是不是临门一脚，而是这条轨迹的自然结果。

如果时间不够，我按这个顺序砍：先砍试听前端（−1.5h），再砍转化卡（−1.0h）。剩下的仍然是一个能跑、能证明核心主张的系统。
