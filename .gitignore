# 凭据 —— 必须先于第一次 commit 生效
.env
.env.local
.env.*.local

# 本地工作台记忆（含连接串与踩坑记录，含敏感信息）
.workbuddy/

# 部署手册 —— 含 ECS 主机、路径与排障记录，**不上传远程**（用户 2026-09-22 明确要求）
deploy.md

# 启停脚本的运行时产物：pid 文件、日志、编译出来的二进制
.run/

# Node
node_modules/
dist/
.vite/

# 本地临时输出（agent 的工具 stdout 落盘目录，只用于回读）
.tmp/

# Go
backend/tmp/
*.exe
*.dll
*.so
*.test
backend/bin/

# 日志与本地文件
*.log
.DS_Store
Thumbs.db
